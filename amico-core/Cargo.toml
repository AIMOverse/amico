[package]
name = "amico-core"
version = "0.1.0"
edition = "2021"
description = "The core of the Amico AI Agent Framework"
repository = "https://github.com/AIMOverse/amico"
license = "MIT"

[dependencies]
rand = "0.8.5"
rig-core = {version = "0.6.1", features = ["all"]}
thiserror = "2.0.11"
tokio = { version = "1.43.0", features = ["time", "rt", "macros"] }
tracing = "0.1.41"

[dev-dependencies]
time = "0.3.37"
